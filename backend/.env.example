# 데이터 소스: mock(기본, 키 불필요) | mfds(식약처 실 API)
DATA_SOURCE=mock

# 식약처 공공데이터포털 활용신청 후 발급받는 서비스키 (DATA_SOURCE=mfds 일 때 필요)
# https://www.data.go.kr 에서 'DUR품목정보', 'e약은요' 활용신청
MFDS_SERVICE_KEY=

# comm 단계 자연어 다듬기: template(기본) | claude | gemini | openai
LLM_PROVIDER=template
ANTHROPIC_API_KEY=
