# 약손(藥손) AI — 진행 점검 리포트 (협의안 대비 현황 진단)

- **작성일**: 2026-06-20
- **목적**: 최신 협의(채택) 방향대로 **잘 진행되고 있는지 점검**하고, **남은 할 일을 명확·상세하게** 정리. 주의·조언·제안 포함.
- **기준선(협의 = 충돌 시 우선)**: GCP(Cloud Run/Cloud SQL) · Next.js(Vercel) · FastAPI · PostgreSQL(+Neo4j는 쟁점) · **OpenAI GPT-4o** · 식약처/심평원 실 공공데이터 · Eval/Gate 가드레일. (근거: 260613 회의록, 확정 아키텍처 구조도)
- **이번 점검에서 실측한 것**: 로컬 `yaksohn-ai/` 코드, Notion 7개 문서+확정본, AI Studio 앱(6/20 라이브), **GitHub 조직 `PromiseBox` 4개 레포 실제 내용**.
- **참고**: 상위 통합 비교는 `약손AI_전체현황_통합검토_2026-06-20.md` 참조. 본 문서는 그 위에서 **"실제 코드 진척 기준 진단 + 액션"**에 집중.

---

## 1. 종합 진단 (한 장 요약)

> **"데이터 기반은 기대 이상으로 탄탄하게 깔렸고, 인프라도 올바른 방향(GCP)으로 착수됐다. 그러나 이 둘을 잇는 애플리케이션 백엔드(FastAPI + 규칙엔진)가 비어 있어, 아직 '공공데이터를 실제 약 조합에 대입해 보여주는 동작 제품'으로 연결되지 않았다. 로컬 Python 자산을 팀 레포로 올려 PostgreSQL과 연결하는 것이 다음 결정적 한 걸음."**

### 워크스트림 스코어카드

| 워크스트림 | 신호 | 실제 진척(실측) | 협의 목표 대비 |
|---|:--:|---|---|
| **데이터 수집·전처리** | 🟢 | 27개 CSV, raw 148.6만 행, 결측 분석 완료 | **거의 완료** |
| **DB(PostgreSQL) 설계·적재** | 🟢 | DDL+한글comment, ETL, 검증SQL, Cloud SQL 이관 덤프(0613) 준비 | **MVP 수준 완료** |
| **인프라(GCP)** | 🟡 | `yakson_project` Terraform GCP IAM 1커밋 | 착수(초기), 방향 일치 |
| **프론트(프로토타입)** | 🟢 | AI Studio 앱(`ui_prototype`, Gemini) 동작 | 프로토타입 존재 |
| **프론트(프로덕션 Next.js)** | 🔴 | 미착수 | **갭 큼** |
| **백엔드 앱(FastAPI+8노드)** | 🔴 | 팀 레포 없음. 로컬 `yaksohn-ai/`에 로직 PoC만 | **최대 병목** |
| **판정엔진 실데이터 연동** | 🔴 | 로컬은 Mock, 실 PostgreSQL과 미연결 | 갭 큼 |
| **GraphRAG / Neo4j** | 🔴 | **코드 0 (다이어그램만)** | 미착수 — 쟁점 |
| **LLM(GPT-4o) 연동** | 🔴 | 로컬 template, 프로토타입은 Gemini | 미착수 |
| **가드레일(Eval/Gate)** | 🟡 | 로컬에 `eval_node`/`gate_node` 동작 PoC | 프로덕션 미이식 |
| **평가(재현율95%/트레이싱)** | 🔴 | 골든셋·측정·LangSmith 미착수 | 미착수 |
| **협업/문서/보안위생** | 🟢 | Notion 충실, data 레포 위생 우수(시크릿 분리·체크섬) | 양호 |

**한 줄 평가**: *방향은 맞다. 데이터·인프라는 잘 가고 있다. 단, "백엔드 공백"이 전체를 정체시키는 단일 최대 리스크다.*

---

## 2. GitHub 실측 — 레포별 현황

| 레포 | 공개 | 언어 | 상태 | 내용 |
|---|---|---|---|---|
| `data` | Private | Python/SQL | 🟢 충실 | PostgreSQL DDL, ETL, 검증SQL, ERD/설계노트, Cloud SQL 이관 덤프(20260613), manifests/체크섬. **검증: CSV 27개·raw 1,485,936행·스키마 yakson** |
| `yakson_project` | Private | HCL(Terraform) | 🟡 착수 | `infra/terraform/` — "feat(infra): Add GCP IAM Terraform configuration" **1커밋**(jazdr/임성빈) |
| `ui_prototype` | Public | TypeScript | 🟢 존재 | **AI Studio Gemini 앱 그대로**(GEMINI_API_KEY, ai.studio 링크). 명시적 "Prototype" |
| `yaksson-ai` | Public | — | ⚪ 최소 | 기획·문서용(README만) |

**여기서 바로 읽히는 사실 3가지**
1. **데이터→PostgreSQL→Cloud SQL** 경로가 이미 코드로 존재(이관 덤프까지). GCP Cloud SQL 타깃 확정.
2. **애플리케이션 백엔드(FastAPI) 레포가 없음.** 즉 "판정 동작"은 로컬 `yaksohn-ai/`(Mock)에만 있고 팀 자산화/실데이터 연동이 안 됨.
3. **실제 DB는 PostgreSQL 단일.** Neo4j/GraphRAG는 어떤 레포에도 코드가 없음 → *다이어그램상의 계획*일 뿐.

---

## 3. 워크스트림별 상세 점검 (현황 · 평가 · 할 일 · 주의)

### 3.1 데이터 & DB — 🟢 가장 앞섬 (잘 진행 중)
**현황**: 14개 데이터셋(파일+API) 수집, 결측 분석 문서화, PostgreSQL 최종 스키마(DDL+한글comment)·ETL·검증 SQL·Cloud SQL 이관 덤프까지 완료. raw 148.6만 행.

**평가**: 협의안의 "로컬 전처리 → GCP DB 적재"를 거의 그대로 이행. 엔지니어링 위생(원본 CSV 제외, `config.local.json` 시크릿 분리, SHA-256·table_counts 검증)이 우수.

**할 일**
- [ ] **금기내용(PROHBT_CONTENT) 결측 처리 정책 확정** — §5 참조(가장 중요).
- [ ] 조인 키 정합성 점검: 품목기준코드(item_seq) ↔ 성분코드(CPNT_CD) 매핑 실패 행 로그화(전처리 문서의 "코드 체계 통일/실패 행 분리" 실제 반영 확인).
- [ ] 골든 약물셋(재현율 측정용 정답셋)을 DB/픽스처로 별도 관리.
- [ ] Cloud SQL 실제 import 1회 리허설(스크립트는 있으나 `PROJECT_ID/INSTANCE` 실값 주입 검증 필요).

**주의·조언**
- ⚠️ **"제외 데이터" 합의 일관성**: 약물유전·혈액 정보는 260613/전처리에서 제외 합의. 그런데 task list엔 "약물-성분-부작용-음식-운동 Knowledge Graph"가 있어 *유전/상호작용 데이터가 다시 필요해질 수 있음*. 제외 결정과 GraphRAG 야심이 충돌 → §4-4와 함께 확정.
- 💡 raw → staging → 최종(yakson) 3계층을 이미 둔 건 좋음. **재현 가능한 ETL(멱등)**인지(같은 입력 재적재 시 동일 결과) 확인 권장(`--force` 경로 포함).

### 3.2 인프라(GCP) — 🟡 착수, 방향 일치
**현황**: `yakson_project`에 GCP IAM Terraform 1커밋. Cloud SQL 이관 스크립트(data 레포)도 GCP 전제.

**평가**: AWS→GCP 전환이 코드 레벨에서 시작됨(올바름). 아직 초기 단계(IAM만).

**할 일**
- [ ] Cloud Run(백엔드), Cloud SQL(PostgreSQL), GCS(이미지/덤프), Secret Manager(키)까지 Terraform 모듈화.
- [ ] Vercel 프로젝트 + 환경변수(서버 키 은닉) 셋업.
- [ ] 배포 파이프라인(최소 수동 `gcloud run deploy` 절차라도 문서화).

**주의·조언**
- ⚠️ **Terraform state 관리**: state 파일에 시크릿이 평문으로 남음 → GCS 백엔드 + state 잠금 사용, 로컬 state를 Git에 올리지 말 것(`.gitignore` 확인).
- 💡 과제 제출용이면 인프라는 **최소 구성**으로. 260605도 "복잡한 운영구조보다 단순 흐름"을 명시 → Cloud Run + Cloud SQL + GCS + Secret Manager 4개면 충분. 과설계 경계.

### 3.3 백엔드 앱(FastAPI + LangGraph 8노드) — 🔴 최대 병목
**현황**: 팀 레포 없음. 로직은 로컬 `yaksohn-ai/`에 존재(규칙엔진 `risk.py`, `eval_node`/`gate_node`, `handoff.py`(중재의견서·QR), `scenarios.py`(Delta/What-if), 8노드 그래프) — 단 **Mock 데이터·template LLM**.

**평가**: *제품의 심장이 비어 있는 상태.* 데이터(🟢)와 프론트(예정) 사이를 잇는 계층이 없어, 현재 "DB는 있는데 그걸 약 조합에 대입해 응답하는 서비스"가 없음.

**할 일 (우선순위 최상)**
- [ ] 로컬 `yaksohn-ai/`를 **팀 레포(예: `backend`)로 승격**.
- [ ] `tools/datasource.py`에 **PostgreSQL DataSource 구현** 추가(`MockDataSource` → `PgDataSource`), `get_default_datasource()` 스위치.
- [ ] FastAPI 라우트: `POST /analyze`(핵심 판정), `POST /extract-drugs`(OCR), `GET/POST/PUT/DELETE /profiles`(복용자 CRUD, Cascade 삭제), `POST /whatif`, `POST /handoff`.
- [ ] `mfds_dur_client` 실호출은 *옵션*(이미 DB 적재했으므로 1차는 DB 조회가 더 빠르고 안정적).
- [ ] Cloud Run 배포(컨테이너화 Dockerfile).

**주의·조언**
- ✅ **희소식**: 출처 강제(`eval_node`)·약사 라우팅(`gate_node`)·중복제거·정렬 등 *심사·법적 안전성의 핵심*이 이미 동작 코드로 있음. 새로 짤 필요 없이 **이식**이 핵심.
- ⚠️ 판정은 **규칙엔진 + DB가 한다. LLM은 절대 판정 금지**(설명만). 로컬 설계 원칙 그대로 유지(협의 §7과 일치).
- 💡 도메인 모델(`Drug/DurRecord/Conflict/Source`)을 DB 스키마 컬럼명과 *명시적 매핑 테이블*로 관리 → 식약처 필드명 변동에도 핵심 로직 불변(스캐폴드 설계 의도 유지).

### 3.4 프론트엔드 — 🟢 프로토타입 / 🔴 프로덕션
**현황**: `ui_prototype`(AI Studio, React+Gemini)이 동작·배포. 협의 스택(Next.js/Vercel) 프로덕션 프론트는 미착수.

**평가**: UX·디자인 자산은 확보(3등급 카드·OCR·확인질문·면책). 단 **Gemini 직호출 구조**라 협의(백엔드 경유 GPT-4o)와 다름. 그대로 제품화하면 방향 이탈.

**할 일**
- [ ] Next.js(Vercel) 신규 앱. 확정 화면정의서의 **3화면(메인/입력/출력)**부터.
- [ ] 모든 AI 호출을 **백엔드(FastAPI)로** 보내도록 변경(프론트에서 LLM 직호출 금지).
- [ ] AI Studio 프로토타입의 **디자인/카피만 차용**, 로직은 백엔드 응답 렌더링으로.

**주의·조언**
- ⚠️ **`ui_prototype`는 "프로토타입"으로 명확히 동결**하고(README에 이미 그렇게 표기됨), 프로덕션과 혼동 금지. 심사·시연에서 어느 것이 "제품"인지 한 개로 정리.
- ⚠️ 과장 카피("실시간 정밀 연동/국가표준 준수/정밀 대조")를 **프로덕션으로 복붙하지 말 것** — 프로토타입의 가장 큰 결함(§6 법규).
- 💡 고령자/보호자 대상 → 큰 글씨·고대비·낮은 인지부하(화면설계서 가이드 반영).

### 3.5 AI: 판정 보강(GraphRAG) + 설명(GPT-4o) — 🔴 다이어그램 단계
**현황**: GraphRAG/Neo4j 코드 0. GPT-4o 연동 0(로컬 template, 프로토타입 Gemini).

**할 일**
- [ ] **GPT-4o 설명 생성** 먼저(`comm.refine_with_llm` 연결) — 저난도·고효과.
- [ ] (Neo4j 채택 시) Knowledge Graph 스키마·적재·Cypher 템플릿·GraphRAG 컨텍스트 빌더.

**주의·조언**
- ⚠️ **GraphRAG는 일정 대비 가장 큰 리스크.** 현재 실데이터는 PostgreSQL로 충분히 핵심 판정(병용금기/PIM/효능군중복/연령/임부/용량/투여기간) 가능. **Neo4j는 식습관·운동/관계경로 차별화용 확장**으로 미루는 것을 강력 권장 → §4-4.
- 💡 GPT-4o 출력은 **반드시 규칙엔진이 만든 사실(출처 포함) 위에서만** 문장화. "LLM이 새 위험을 추가/추측"하면 환각 → `eval_node`가 프로필 외 약물·무출처를 차단하도록 출력도 검증.

### 3.6 가드레일·평가 — 🟡 PoC 있음, 미이식
**현황**: 로컬에 `eval_node`(출처강제·환각차단)·`gate_node`(약사 라우팅) 동작. 재현율 측정·트레이싱 미착수.

**할 일**
- [ ] 백엔드 이식 후 **골든셋 대비 재현율 측정 스크립트**(목표 95%+).
- [ ] **환각 0건** 검증 케이스(프로필 외 약물 참조 차단).
- [ ] 트레이싱 **LangSmith vs Langfuse 택1 확정** 후 연결.

**주의·조언**
- 💡 **이게 차별화 포인트다.** 다른 팀이 "LLM이 답해주는 약 상담"을 만들 때, 약손은 "출처 있는 규칙판정 + 환각 차단 + 약사 라우팅"을 *증명*할 수 있음. 데모/발표에서 **Eval 통과 화면·출처 표기**를 반드시 1컷 보여줄 것.

### 3.7 협업·보안 위생 — 🟢 양호 (계속 유지)
**현황**: Notion 문서 체계 충실. data 레포가 시크릿 분리·체크섬·manifests로 모범적.

**주의·조언**
- 🔴 **OpenAI 공유키(40달러) 절대 커밋 금지.** 서버(Cloud Run)에서만, GCP Secret Manager/.env로. 프론트(Vercel)엔 절대 노출 금지(반드시 백엔드 경유).
- ⚠️ `ui_prototype`이 Public이고 GEMINI_API_KEY를 `.env.local`로 받음 — **레포에 실제 키가 안 올라갔는지 히스토리 점검**(한 번이라도 커밋됐다면 폐기·재발급).
- 💡 data 레포의 위생 수준을 backend/frontend 레포 표준으로 채택(.gitignore·시크릿 예시파일·README 적재절차).

---

## 4. 방향 정합성 리스크 (= "잘 가고 있나"에 대한 경고)

1. **🔴 백엔드 공백이 통합을 막음.** 데이터(🟢)·인프라(🟡)·프론트프로토(🟢)가 따로 존재하고 *이어붙이는 계층이 없음*. 누군가 "백엔드 오너"를 지정하고 로컬 자산 이식을 즉시 시작해야 함.
2. **🔴 다이어그램 vs 현실 — Neo4j/GraphRAG.** 확정 아키텍처엔 그려져 있으나 코드 0. "그림은 화려한데 구현은 PostgreSQL"인 상태가 길어지면 발표 때 *과장 아키텍처* 비판을 받을 수 있음. **그릴 거면 만들고, 안 만들 거면 MVP 다이어그램에서 빼라.**
3. **🟡 세 갈래 통합 오너십 부재.** 로컬 Python / AI Studio 프로토 / 팀 레포(data·infra)가 한 사람 머릿속에서만 연결됨. **단일 "제품 레포 + README 아키텍처"**로 SSOT(단일 진실원천) 확립 필요.
4. **🟡 미확정 쟁점이 동시 착수를 막음.** 웹 vs 모바일, 화면 3 vs 6, Neo4j 필수 여부 — 안 정해지면 프론트/AI 워크스트림이 못 움직임. §8에서 즉시 결정 권장.
5. **🟡 LLM 일원화 미완.** 프로토는 Gemini, 협의는 GPT-4o, 로컬은 template. 제품 경로에서 **GPT-4o 단일**로 수렴시키고 프로토의 Gemini는 프로토에 가둘 것.
6. **🟡 평가 지표 미정의.** "재현율 95%"의 *분모(골든셋)·측정법*이 없으면 달성 주장 불가. 조기 정의 권장.

---

## 5. ⚠️ 데이터 품질 심층 주의 (실데이터 적재 후 가장 중요)

전처리 문서의 결측 분석에서 **서비스 신뢰성에 직결되는 위험**이 보임:

| 테이블 | 금기/주의내용(PROHBT_CONTENT) 결측 | 함의 |
|---|---|---|
| 병용금기 | 2.97% (양호) | 사유 문구 대부분 존재 |
| 특정연령대금기 | 2.97% (양호) | 양호 |
| 임부금기 | 10.40% | 일부 사유 공백 |
| 용량주의 | 40.11% | 사유 공백 다수 |
| **노인주의(PIM)** | **76.79%** | **대부분 사유 공백** |
| **효능군중복** | **93.33%** | **거의 전부 사유 공백** |
| 투여기간주의 | 97.96% | 거의 전부 공백 |

**왜 위험한가**: 약손이 화면에 보여주는 "근거 문구(사유)"가 PROHBT_CONTENT다. 그런데 **가장 흔하게 뜨는 노인주의·효능군중복에서 사유가 거의 비어 있음.** 처리 정책이 없으면 (a) 빈 사유로 신뢰 저하, 또는 (b) LLM이 사유를 지어내는 환각 — 둘 다 치명적.

**권고 처리 정책 (반드시 합의)**
- 사유 공백 시 **카테고리 표준 문구**로 대체: 예) 노인주의 → "고령자에서 주의가 필요한 약물로 등재되어 있습니다(식약처 노인주의 목록). 사유 상세는 약사·의사 확인 권고." (로컬 코드의 `rec.prohibit_content or "고령자 주의 약물로 등재되어 있습니다."` 패턴 그대로 이식)
- **출처(테이블·공고번호·공고일자)는 항상 표기** — 사유가 비어도 "어디 목록에 등재됨"은 사실이므로 신뢰 가능.
- **LLM에 사유 생성 금지.** 빈 사유를 GPT-4o로 채우지 말 것(환각). 표준 문구 + 약사 라우팅으로만.
- 효능군중복은 "같은 효능군 2개 이상 → 중복 가능성"이라는 *규칙 자체가 근거*이므로 사유 텍스트 없이도 설명 가능.

**기타 데이터 주의**
- intrcQesitm(상호작용) 결측을 **"안전"으로 해석 금지**(전처리 문서도 명시) — "정보 없음"으로.
- 노인주의 112행·효능군중복 405행은 **성분 단위 소형 참조표** → 실제 매칭은 품목(23,509)과 성분코드 조인으로 확장됨. 조인 누락 시 위험 미탐지 가능 → 조인 커버리지 점검.

---

## 6. 주의·조언·제안 종합

### 법규·안전 (최우선)
- ⚖️ **표시·광고**: "공식/실시간 연동/국가표준 준수/정밀 대조" 등 단정 표현은 사실(LLM/규칙 추론)과 달라 *허위·과장* 리스크. 프로덕션 카피는 "식약처 등재 정보를 **참고**한 위험 가능성 안내 보조 도구"로.
- ⚖️ **의료기기 규제 경계**: 식습관·운동은 "추천/치료"로 가면 규제영역(260605 경고). **"복용 중 주의 정보" + "약사·의사 상담"**으로만 한정.
- ⚖️ **진단·처방 단정 금지**: 중단/증량/감량/신규복용/진단 어휘 금지(로컬·협의 공통 가드레일). 출력 검증에 금칙어 필터 추가 권장.
- 🔐 **개인정보**: MVP는 별칭·나이·성별 최소수집. 알레르기·질환 등 민감정보는 *선택+동의*. 약봉투 이미지는 **OCR 텍스트만 저장, 원본 미보관**(260605 권고)이 가장 안전.

### 기술·운영
- 🔐 **공유 API 키는 서버 전용**(프론트 노출 0). Secret Manager. 레포 히스토리 키 점검.
- 🧰 **공공 API 폴백**: 1차는 DB 조회(이미 적재)라 API 의존도 낮음 — 좋음. OCR/LLM은 외부 의존이므로 타임아웃·레이트리밋·실패 시 폴백(샘플/안내) 준비.
- 🧪 **재현율 측정 자동화**: 골든셋 고정 → CI에서 회귀. 로컬 13개 테스트 자산을 백엔드 레포로 가져가 확장.
- 🗂️ **SSOT 확립**: "제품 레포 1개 + 아키텍처 README"로 세 갈래 혼선 종식. Notion=의사결정/문서, GitHub=코드/스펙.

### 일정·범위 (공모전 관점)
- 🎯 **MVP를 좁혀라**: 3화면 + 핵심 7종 판정 + 출처/약사라우팅 + GPT-4o 설명. 이게 *심사에서 이기는 최소집합*.
- 🎯 **차별화 1컷**: "정부 데이터를 내 부모 약에 대입 → 출처 달린 경고 → Eval 통과/환각 0" 데모. 경쟁 우위는 화려함이 아니라 **신뢰성**.
- 🛑 **과욕 경계**: Neo4j·모바일 네이티브·식습관/운동 풀구현은 *시간 있으면* 확장. MVP 필수 아님.

---

## 7. 다음 1~2주 권장 액션 (구체)

**즉시(이번 주)**
1. 🔴 **백엔드 오너 지정 + 로컬 `yaksohn-ai/` → 팀 `backend` 레포 승격.**
2. 🔴 **`PgDataSource` 구현** — 적재된 PostgreSQL에서 `resolve_drug`/`dur_records` 조회. (Mock과 동일 인터페이스라 교체만 하면 기존 규칙엔진 그대로 동작)
3. 🔴 **PROHBT_CONTENT 결측 처리 정책 합의**(§5) 후 코드 반영.
4. 🟡 **미결정 3종 확정 회의**(웹/모바일, 화면 3/6, Neo4j 필수 여부) — §8.

**다음 주**
5. FastAPI `POST /analyze` + Cloud Run 1차 배포(실데이터로 동작하는 엔드포인트).
6. GPT-4o `refine_with_llm` 연결(설명만, 출처 위에서).
7. Next.js 3화면 골격 + `/analyze` 연동(프로토타입 디자인 차용).
8. 골든셋 + 재현율 측정 스크립트 1차.

---

## 8. 팀 합의 대기 (결정해야 움직임)

- [ ] **클라이언트 형태**: 반응형 Next.js 웹(권장) vs 네이티브 모바일앱
- [ ] **MVP 화면**: 3화면(권장) vs 6화면(식습관·운동 포함 시점)
- [ ] **Neo4j/GraphRAG**: MVP 제외(권장, PostgreSQL로 충분) vs 즉시 착수
- [ ] **AI Studio 프로토타입**: 동결(권장) vs 유지보수(시 과장문구·출처·localStorage 보정 필수)
- [ ] **OCR/이미지**: GCP Vision/Document AI + (저장 시) GCS, 또는 OCR 텍스트만 저장
- [ ] **트레이싱**: LangSmith vs Langfuse
- [ ] **PROHBT_CONTENT 결측 표준 문구** 문안
- [ ] `약손AI_기획서.docx`(확정문서 첨부) 내용과 본 점검 정합성 — 미열람

---

### 부록 — 점검 출처
- 로컬: `yaksohn-ai/`(코드), `약손AI_ClaudeCode_지시문_v2.md`, `약손AI_AIStudio앱_현황검토_2026-06-18.md`
- Notion: AI Project(약속상자) 7개 문서 + 확정본(화면정의서 3화면 목업 / 아키텍처 구조도 GraphRAG)
- AI Studio: 앱 6b1adbc8…(6/20 라이브, 과장문구·Gemini 유지 확인)
- GitHub `PromiseBox`: `data`(🟢), `yakson_project`(🟡 Terraform), `ui_prototype`(Gemini 프로토), `yaksson-ai`(문서)
