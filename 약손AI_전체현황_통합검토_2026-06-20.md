# 약손(藥손) AI — 전체 현황 통합 검토

- **작성일**: 2026-06-20
- **검토 범위**: ① 로컬 초기 문서(`약손AI_ClaudeCode_지시문_v2.md`, `yaksohn-ai/` 코드, `약손AI_AIStudio앱_현황검토_2026-06-18.md`) → ② Notion `AI Project(팀명: 약속상자)` 전체 7개 문서 + 확정본(화면정의서·아키텍처 구조도) → ③ Google AI Studio 배포 앱
- **충돌 처리 원칙**: 초기 문서와 진행 내용이 충돌하면 **Notion의 최신 협의 내용을 기준**으로 삼음 (사용자 지시).
- **팀 역할 주의**: 로컬 Python(`yaksohn-ai/`)은 수정 가능 자산, **AI Studio Gemini 웹앱은 팀 과제(타 팀원 소유)로 직접 수정 대상 아님** — 아래 AI Studio 관련 지적은 "수정 지시"가 아니라 협의안과의 **정합성 갭**으로 읽을 것.

---

## 0. 결론 한 줄

> **세 갈래가 따로 존재한다.** ① 로컬 Python = *동작하는 규칙엔진 PoC*, ② AI Studio 앱 = *완성도 높은 UX 프로토타입(단, Gemini·출처부재·과장문구)*, ③ Notion 최신 협의안 = *실제 제출용 목표 아키텍처(GCP + Next.js + FastAPI + PostgreSQL/Neo4j + GPT-4o + 실 공공데이터)*.
> **핵심 과제는 "③ 목표를 향해 ①의 엔진을 이식하고 ②의 UX를 차용하되, 클라우드(AWS→GCP)·LLM(Claude/Gemini/Bedrock→OpenAI)·데이터(Mock→실데이터) 충돌을 최신 협의대로 수렴시키는 것".**

---

## 1. 세 갈래 현황 한눈에 보기

| | ① 로컬 Python `yaksohn-ai/` | ② Google AI Studio 앱 | ③ Notion 최신 협의안(목표) |
|---|---|---|---|
| 성격 | 로직 PoC (오늘 구현용) | UX 프로토타입(배포됨) | 제출용 프로덕션 설계 |
| 프론트 | Chainlit(데모) | React(Vite) | **Next.js(Vercel)** / task list엔 모바일앱(Flutter·RN) |
| 백엔드 | 순수 함수 + LangGraph | Express `server.ts` | **FastAPI on GCP Cloud Run** |
| 판정 방식 | **규칙기반(결정론)** | **LLM 추론(Gemini)** | **규칙기반 + GraphRAG, LLM은 설명만** |
| LLM | template(키 불필요) | gemini-3.5-flash | **OpenAI GPT-4o** |
| 데이터 | 가상 예시(Mock/골든) | 모델 파라메트릭 지식 | **실 공공데이터 14종 전처리** |
| DB | JSON 파일 | 브라우저 localStorage | **PostgreSQL + Neo4j(GraphRAG) 하이브리드** |
| 출처(provenance) | **모든 Conflict에 Source 강제 + eval 차단** | **출처 필드 없음** | **Eval Gateway로 출처 강제** |
| OCR | 없음 | Gemini 비전 | (확정도엔 미명시 — GCP Vision/Document AI 필요) |
| 상태 | pytest 13개 통과, 동작 | 배포·동작(6/20 확인) | 설계 확정, 구현 착수 단계 |

---

## 2. 초기 문서 ↔ 최신 협의 충돌 정리 (최신 Notion 기준)

> 사용자 지시대로 **충돌 시 최신 협의(특히 260613 회의록 + 확정 아키텍처 구조도)가 우선**.

| 항목 | 초기/중간 내용 | **최신 협의(채택)** | 출처 |
|---|---|---|---|
| **클라우드** | AWS native (CloudFront·Lambda·Step Functions·Bedrock·Textract·RDS) — 260605 | **GCP** (Cloud Run, Jake 계정 생성·팀 초대) | 260613 회의록, 아키텍처 계획 |
| **LLM** | Claude(로컬 README TODO) / Bedrock(260605) / Gemini(AI Studio) | **OpenAI 계열(GPT-4o)** — 검증·가격효율·벤치마크. Claude는 비용↑ 우선순위↓ | 260613 회의록, 아키텍처 구조도 |
| **프론트** | React+Express(AI Studio), Chainlit(로컬) | **Next.js(Vercel) + API Routes로 키 은닉** | 아키텍처 계획 |
| **백엔드** | (로컬은 순수 함수) | **FastAPI** (Cloud Run에서 LangGraph 8노드 실행) | 아키텍처 계획, task list |
| **DB** | Mock JSON(로컬) / localStorage(AI Studio) / RDS·DynamoDB(260605) | **PostgreSQL(정형) + Neo4j(Knowledge Graph) 하이브리드** | 아키텍처 구조도, task list, 화면설계서 |
| **판정 방식** | 규칙기반(로컬) vs LLM추론(AI Studio) | **규칙기반 + GraphRAG가 판정, LLM은 설명 생성만** | 260605·아키텍처 구조도 |
| **데이터** | 가상 예시(로컬), 모델 지식(AI Studio) | **실 공공데이터 14종 수집·전처리 후 DB 적재** | 260613, 데이터 전처리 문서 |
| **이미지 저장** | — | S3(260605) → **GCP면 GCS로 대체 필요(미확정)** | 260605 + GCP 전환 |
| **OCR 엔진** | — | Textract(260605) → **GCP Vision/Document AI 필요(미확정)** | 260605 + GCP 전환 |
| **음식·운동** | 로컬·AI Studio 모두 없음 | **식습관·운동 리포트 포함**(GraphRAG, "추천" 아닌 "주의 안내" 수준) | 화면설계서, 아키텍처 구조도 |

---

## 3. Notion 내부에 아직 남은 "확정 필요" 쟁점

> 최신 문서들 사이에서도 서로 어긋나는 부분이 있어, 구현 착수 전 **팀 합의로 못 박아야 할 항목**.

1. **웹 vs 모바일 앱**
   - 아키텍처 계획 = **Next.js 웹**, 화면정의서(확정 목업) = 모바일 화면 3종, task list·화면설계서 = **모바일 앱(Flutter/React Native)**, 260605 = "모바일 대응 웹앱 우선".
   - 👉 **권장**: *반응형 Next.js 웹앱 우선*(260605·아키텍처와 일치), 네이티브 앱은 후순위 확장. → **명시적 확정 필요.**

2. **화면 범위(MVP vs 확장)**
   - 확정 화면정의서 = **3화면**(메인/입력/출력) · 260605 = 3화면 MVP · 화면설계서 = **6화면**(+ 식습관·운동 리포트).
   - 👉 **권장**: MVP = 3화면, 확장 = 6화면으로 **단계 구분**.

3. **식습관·운동 리포트**
   - 260605에서 *규제(의료기기)·알레르기 우려로 MVP 제외* 결정 → 이후 화면설계서·아키텍처엔 다시 포함.
   - 단 260605도 "추천이 아닌 *주의 안내* 수준"은 허용. 화면설계서도 "피해야 할 음식/운동 주의사항" 표현이라 **양립 가능**.
   - 👉 **권장**: "추천" 단어 배제, "복용 중 주의 정보 + 약사·의사 상담" 프레이밍으로 확정.

4. **Neo4j / GraphRAG 도입 여부**
   - 아키텍처 구조도·화면설계서엔 포함, 그러나 task list는 "*새로 제안된*"이라 표기, 260613 회의록은 "하나의 DB에 여러 테이블(RDBMS)"만 언급.
   - 👉 Graph DB는 구현 난이도·일정 리스크가 큼. **MVP 필수 여부 재확인** 권장(PostgreSQL만으로 핵심 판정 가능 → Neo4j는 식습관·운동/관계경로 차별화용 확장으로).

5. **공유 OpenAI API 키 보안**
   - 260613: 40달러 공유 키 운용. 👉 **GitHub 커밋 금지**, 서버(FastAPI/Cloud Run)에서만 사용, GCP Secret Manager·.env로 관리.

---

## 4. 자산 재사용 전략 (③ 목표로 수렴시키는 법)

| 자산 | 목표에서의 역할 | 그대로 살릴 것 | 바꿔야 할 것 |
|---|---|---|---|
| **① 로컬 Python** | **FastAPI 백엔드의 코어** | `risk.py`(규칙엔진), `nodes.eval_node`(출처강제), `gate_node`(약사 라우팅), `handoff.py`(중재의견서·QR), `scenarios.py`(Delta·What-if), 8노드 그래프, 도메인 모델 | Mock→PostgreSQL 실데이터, template→GPT-4o(`refine_with_llm`), CLI/Chainlit→FastAPI 라우트 |
| **② AI Studio 앱** | **Next.js 프론트 디자인 레퍼런스** | 3등급(위험/주의/정상) 카드 UX, OCR 입력 플로우, 확인질문·미확인약물 분리, 강한 면책, 온화한 톤 | Gemini→백엔드 경유 GPT-4o, localStorage→서버 DB, **과장문구 수정**, 출처 라벨 추가 |
| **③ 확정 화면정의서** | **MVP UI 기준** | 메인/입력/출력 3화면 흐름, PIM·병용금기 예시 카드 | — |

> **요지**: ①이 이미 "출처 강제 + 환각 차단 + 약사 라우팅"이라는 *심사·법적 안전성의 핵심*을 동작 코드로 갖고 있음 → 이걸 FastAPI로 이식하는 게 가장 빠르고 강한 길. ②의 약점(출처부재·과장)이 정확히 ①의 강점이라 **②의 프론트 + ①의 백엔드 결합**이 정답.

---

## 5. 개선 필요 (즉시)

### 5.1 AI Studio 앱 — 협의안과의 정합성 갭 (※ 팀 소유, 협의 후 담당자 조치)
- 🔴 **P1 과장문구 잔존(6/20 확인)**: "식약처 & 건강보험심사평가원(DUR) 공식 가이드라인 **실시간 정밀 연동**", "**국가 표준 준수**", "정확하게 분석 **정밀 대조**" → 실제는 Gemini 학습지식 추론. **"AI가 DUR·Beers 기준을 참고해 추론하는 보조 도구"** 수준으로 완화 필요(표시광고 리스크).
- 🔴 **P2 출처 필드 부재**: 결과 항목에 근거(고시·기준명) 없음 → 최소 항목별 *"AI 추론 결과 · 식약처 원문 대조 아님"* 라벨. (협의안은 Eval Gateway로 출처 강제 → 정면 충돌)
- 🟡 **P4 localStorage 평문 저장**: 협의안은 서버 DB(PostgreSQL). 데모 유지 시 최소화·마스킹.
- 🟡 **모델 불일치**: Gemini ↔ 협의 OpenAI. → AI Studio 앱을 *프로토타입으로 종료*할지, 유지할지 팀 결정 필요.
- ⚪ **공개·편집 가능 공유 링크**("Anyone with this public link can access and edit") 권한 점검.

### 5.2 로컬 Python — 목표 전환 준비
- `.gitignore` 부재(노이즈 파일 다수 추적 중) → 추가 권장(`.venv/`, `__pycache__/`, `.pytest_cache/`, `outputs/`, `.DS_Store`).
- `tools/mfds_dur_client.py` 실호출 활성화(현재 골격) + 응답 필드 → 도메인 모델 매핑 보정.
- `mock_data.py` 가상 데이터 → 전처리된 실데이터(PostgreSQL 조회)로 교체.
- `comm.refine_with_llm` template → GPT-4o 연결(서버에서 키 관리).
- FastAPI 래핑(`POST /analyze`, `/extract-drugs`(OCR), `/profiles` CRUD).

---

## 6. 추가 진행 필요 (협의안 구현 로드맵)

### 6.1 데이터 (현재 가장 진척됨 — 전처리 거의 완료)
- 14종 공공데이터 전처리 마무리(인코딩→만료품목→결측→코드통일→중복→모순→로그 순서 문서화됨).
- **PostgreSQL 스키마 설계·적재**: 테이블 분리 — 병용금기 / 노인주의(112행) / 임부금기(1,433행) / 효능군중복(405행) / 용량주의(708·7,041행) / 투여기간주의(98·410행) / 특정연령대금기(1,816행) / 수유부주의(548행) / e약은요 / 성분별 1일최대투여량 / DUR품목정보(23,509행).
- 공통 ID(품목기준코드·성분코드)로 조인 키 설계.
- **제외 확정**: 약물유전 정보·혈액 관련(결측 과다·서비스 부적합 — 260613/전처리 문서 일치).
- 골든 약물셋(재현율 측정용 정답셋) 구축.

### 6.2 백엔드/AI
- FastAPI + LangGraph **8노드** 파이프라인(로컬 이식) on **Cloud Run**.
- 규칙 기반 위험 판정(로컬 `risk.py` 재사용) + 실데이터 연동.
- (Neo4j 채택 시) GraphRAG 컨텍스트 빌더 + 템플릿화 Cypher 쿼리 + Knowledge Graph(약물-성분-부작용-음식-운동) 적재.
- GPT-4o: 보호자용 쉬운 설명 + 약사 질문지 + 인계자료 생성(판정은 X).
- ETL: Apache Airflow(공공데이터 수집·전처리 스케줄러) — 확정 아키텍처에 명시.

### 6.3 프론트엔드
- Next.js(Vercel) — 3화면 MVP(메인/입력/출력) → 6화면 확장(+식습관·운동).
- API Routes로 GCP/OpenAI 키 은닉.
- AI Studio 디자인 차용(3등급 카드·OCR·확인질문·면책).
- 고대비·큰 글씨(고령자/보호자 접근성).

### 6.4 검증·운영
- 골든셋 대비 부적절약물/중복 탐지 **재현율 95%+**.
- **Eval Gateway**(프로필 외 약물 참조 차단·환각 0건) — 로컬 `eval_node` 이식.
- **진단 방지 Gate**(심각도 상 → 약사·의사 라우팅) — 로컬 `gate_node` 이식.
- LangSmith(아키텍처 계획) 또는 Langfuse(task list) 트레이싱 — **둘 중 택1 확정 필요**.
- 인프라: GCP 프로젝트(Jake), 팀 GitHub(`github.com/PromiseBox`), Secret Manager.
- 배포: Cloud Run + Vercel. (모바일 채택 시 Firebase App Distribution/TestFlight.)

---

## 7. 권장 우선순위 (공모전·과제 일정 역산)

가치 핵심 = **"정부 공공데이터를 실제 약 조합에 대입 + 출처 + 약사 라우팅"**. 이를 가장 빨리 동작시키는 순서:

1. **데이터 → PostgreSQL 적재** (이미 전처리 진척 큼 — 즉시 착수)
2. **로컬 규칙엔진 → FastAPI 이식 + 실데이터 연동** (이미 동작하는 자산 활용 → 빠른 백엔드)
3. **Next.js 프론트(AI Studio 디자인 차용) + 백엔드 연동** (3화면 MVP)
4. **GPT-4o 설명 생성 + Eval/Gate 가드레일** (심사·안전성)
5. **GraphRAG / 식습관·운동 리포트** = 시간 여유 시 차별화 확장

> ①②는 기존 자산 재사용이라 단기 완성 가능. ④의 Eval/Gate가 다른 팀 대비 *차별화 포인트(환각 통제·출처)*이므로 데모에서 반드시 부각.

---

## 8. 미해결·확인 필요 목록 (팀 의사결정 대기)

- [ ] 웹(Next.js) vs 네이티브 모바일앱(Flutter/RN) — 클라이언트 형태 확정
- [ ] MVP 화면 범위 3 vs 6 (식습관·운동 포함 시점)
- [ ] Neo4j/GraphRAG MVP 필수 여부
- [ ] AI Studio Gemini 앱 — 프로토타입 종료 vs 유지(유지 시 과장문구·출처·localStorage 보정)
- [ ] OCR 엔진(GCP Vision/Document AI) + 이미지 저장(GCS) 방식
- [ ] 트레이싱 도구 LangSmith vs Langfuse
- [ ] 공유 OpenAI 키 보안 운용(Secret Manager) 책임자
- [ ] `약손AI_기획서.docx`(확정문서 첨부, 27.2KiB) 내용과 본 정리의 정합성 — 미열람(첨부파일)

---

### 부록 — 검토한 Notion 문서 목록
1. AI Project(팀명: 약속상자) — 루트(팀원/깃헙/참고 레포)
2. 과제/회의 확정 문서 — 기획서.docx + **화면정의서(3화면 목업)** + **아키텍처 구조도(GraphRAG)**
3. 화면 설계서 — 6화면 상세 정의(식습관·운동 포함)
4. task list — WBS(하이브리드 DB·모바일앱·GraphRAG·GPT-4o)
5. 260605 회의록 — 초기 합의(AWS·Bedrock·3화면 MVP·음식운동 제외)
6. 260613 회의록 — **최신 결정(AWS→GCP, OpenAI, 공통 DB, 팀 레포)**
7. 데이터 전처리(+링크) — 14종 데이터 결측 분석 + data.go.kr 링크·담당자
